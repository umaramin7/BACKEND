<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block px-5 py-3 w-full rounded-lg outline-none bg-zinc-700" placeholder="title goes here" name="title">
                <textarea class="block px-5 py-3 w-full outline-none rounded-lg mt-2 resize-none bg-zinc-700" placeholder="write your task details" name="details"></textarea>
                <input class="mt-2 bg-green-700 rounded p-4" type="submit" value="Add Task">
            </form>
        </div>
        <div class="tasks flex gap-3 flex-wrap  p-10 ">
            <% if(files.length > 0) { %>
            <% files.forEach(function(val){ %>
                <div class="task w-72 px-5 py-2 rounded-lg bg-zinc-700">
                <h1 class="text-3xl tracking-tighter text-white"><%=val%></h1>
                <div class="flex justify-between">
                <a href="/file/<%= val %>" class="text-blue-500 inline-block mt-3">Read more</a>
                <a href="/edit/<%= val %>" class="text-yellow-600 inline-block mt-3">Edit</a>
                </div>
                
            </div>
             <div class="task hidden w-72 px-5 py-2 rounded-lg bg-zinc-700">
                <h1 class="text-3xl tracking-tighter text-white">Some task goes here</h1>
                <a href="" class="text-blue-500 inline-block mt-3">Read more</a>
            </div>
             
             <div class="task hidden w-72 px-5 py-2 rounded-lg bg-zinc-700">
                <h1 class="text-3xl tracking-tighter text-white">Some task goes here</h1>
                <a href="" class="text-blue-500 inline-block mt-3">Read more</a>
            </div>
            <%})%>
            <% } else{ %>
                <h3  class="text-zinc-700">Nothing Yet</h3>
                <%}%>
            
            
        </div>
    </div>
</body>
</html>